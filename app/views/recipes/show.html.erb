<%= render :partial => 'recipe' %>
<% if user_signed_in? %>
  <%= link_to 'Queue Recipe', {:controller => 'queued_recipes',
                           :action => 'create',
                           :recipe_id => @recipe.id,
                           :user_id => current_user.id},
                           :method => 'post' %>
<% end %>